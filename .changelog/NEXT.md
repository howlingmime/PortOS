# Unreleased Changes

## Added

## Changed

## Fixed
- Unblocking tasks from the banner now reactively updates both the banner and task list without requiring a page reload
- PortOS self-build no longer causes socket hangup by ignoring package-lock.json in PM2 watch; UI shows graceful toast on self-build connection loss

## Removed
