# v1.25.1

## Added

- `pm2:save` and `pm2:startup` npm scripts for OS boot persistence
- Auto `pm2 save` after `npm start` to persist process list

## Fixed

- `.gitignore` workspace sub-package lock files (browser, client, server)
- `update.sh` use subshells for cd safety
